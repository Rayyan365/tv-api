document.querySelector.addEventListener("click", (function)
    const searchButton = document.(".search-box"));
    searchButton.addEventListener('click', searchNetflix);
  ;
  
  function searchNetflix() {
    const searchValue = document.querySelector(".movies-section").value;
    const searchResult = document.querySelector('.search-results');
   
    searchResult.innerHTML = 'Search' + searchValue;

  }
  //ratings 
  document.querySelector(".ratings");
  ratings.array.forEach(element => {
    
  });